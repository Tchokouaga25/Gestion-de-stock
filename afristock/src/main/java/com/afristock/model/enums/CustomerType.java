package com.afristock.model.enums;

/**
 * Type de client.
 */
public enum CustomerType {
    /** Client particulier (vente au détail). */
    PARTICULIER,
    /** Client entreprise (vente en gros, B2B). */
    ENTREPRISE
}
